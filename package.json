{
  "name": "maidecorate-farmasi-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "sync:pages": "node scripts/generate-pages.mjs",
    "predev": "npm run sync:pages",
    "dev": "vite --host 0.0.0.0",
    "prebuild": "npm run sync:pages",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "react": "18.3.1",
    "react-dom": "18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "4.3.4",
    "vite": "5.4.11"
  }
}
